# Postgres connection for the repository layer and pipeline.
DATABASE_URL=postgres://rockygpt:rockygpt@localhost:5432/rockygpt
# Optional staging-only gate. When set, every non-health request must include it.
STAGING_SERVICE_TOKEN=
